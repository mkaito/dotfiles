#!/usr/bin/env zsh
set -euo pipefail

if [[ ! -t 0 ]]; then
  notify-send "Screengrab activated" "Drag region"
fi

maim -s | xclip -selection clipboard -t image/png

notify-send 'Screenshot' 'Region copied to clipboard'
