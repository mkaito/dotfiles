#!/usr/bin/env zsh
set -euo pipefail

tmp=$(mktemp --suffix=.png)
trap 'rm -f "$tmp"' EXIT

maim -i "$(xdotool getactivewindow)" "$tmp"
xclip -selection clipboard -t image/png -i "$tmp"

notify-send "Window Screenshot" "Window screenshot taken, copied to clipboard." || true
