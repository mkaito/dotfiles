text/html; /home/chris/dev/dotfiles/bin/browser %s; test=test -n "$DISPLAY"; needsterminal;
text/html; w3m -I %{charset} -T text/html; copiousoutput;

image/*; imv %s;
application/zip; unzip %s;
application/tar; tar xf %s;
application/pdf; zathura %s
